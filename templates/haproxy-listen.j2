

# tcp listeners ####################################
# for version {{ _haproxy_major_version }}.{{ _haproxy_minor_version }}
{% for config in haproxy_tcp_listen %}

{# check for config version #}
{# listen changed in 1.6 #}
{% if _haproxy_major_version == '1' and _haproxy_minor_version >= '6' %}
listen {{ config.name }} 
    bind {{ config.bind_address }}:{{ config.bind_port }}
{% else %}
listen {{ config.name }} {{ config.bind_address }}:{{ config.bind_port }}
{% endif %}
    balance leastconn
    mode tcp
    option tcplog
{# options from config #}
{% if config.options is defined and config.options[0] is defined %}
{% for opt in config.options %}
    {{ opt }}
{% endfor %}
{% endif %}
{% set backup = config.backend.backup|default(False) %}
{% for server in config.backend.servers %}
    server {{ server }} {{ server }}:{{ config.backend.port }} {{ config.backend.check|default(haproxy_default_backend_check) }} {% if not loop.first and backup %}backup{% endif %} 
{% endfor %}
{% endfor %}
